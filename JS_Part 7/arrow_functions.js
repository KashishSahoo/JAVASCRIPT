const sum=(a,b)=>{
console.log(a+b);
};

const cube=(n)=>{
    console.log(n*n*n);
    };

cube(2);

const power=(a,b)=>{
    console.log(Math.pow(a,b));
    };

power(2,4);

const mul=(a,b)=>(
    a*b
);

let ans=mul(16,2);
console.log(ans);

const square=(n)=>{
    return n*n;
};

let anss=square(16);
console.log(anss);
    